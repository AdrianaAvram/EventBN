<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="EventBN.MainPage">

   
        
        
        <ListView x:Name="MyListView" HasUnevenRows="False" RowHeight="50">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout>
                            <Label Text="{Binding Nume}" FontAttributes="Bold" FontSize="20"/>
                            <Label Text="{Binding Data}" FontAttributes="Italic"/>
                            <Label Text="{Binding Localitate}" FontAttributes="Italic"/>
                            <Label Text="{Binding Informatii}" FontAttributes="Italic"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
       
   

</ContentPage>
